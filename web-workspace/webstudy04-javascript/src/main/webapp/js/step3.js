/*
	step3-function.html 에서 사용하는 함수를 정의
*/

function hello() {
	console.log("hello 함수 실행");
	alert("hello 함수 실행");
}

function bye(message) {
	alert(message);
}

function testParam(info, num) {
	alert(info + " " + num);
}